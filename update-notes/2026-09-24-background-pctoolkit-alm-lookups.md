# More reliable background connections and ALM lookups

- PC Toolkit now connects and checks its session in the background when enabled, without opening an automatic sign-in window.
- ALM review now looks up every serial, including pending returns, and retries serials whose model details are missing.
